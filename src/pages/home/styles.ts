import styled from "styled-components";

export const StyledHome = styled.div`
    .isLoading {
        display: flex;
        justify-content: flex-start;
        align-items: center;
        flex-direction: row;
    }

    .spinner {
        margin-right: 1rem;
    }
`;
